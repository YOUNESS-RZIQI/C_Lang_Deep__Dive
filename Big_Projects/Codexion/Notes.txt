1) in @dongle.c, there is sim not used ??


2) why the time long long why not just long ?

3) the parameters of creat,join, mutex, cond that i do not know ??

4) why we do have wake_all_dongles ?

5) 	if (dongle->queue_size >= 2)
		return ;
        do we push to the heap more then two times ?

6) what is priority ? in the heap_push

			usleep((unsigned int)(remaining * 1000));
7) why : 